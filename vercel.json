{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd backend && npx --yes pnpm@10.18.0 install --frozen-lockfile",
  "buildCommand": "cd backend && pnpm run build && rm -rf ../api/backend-dist && mkdir -p ../api/backend-dist && cp -r dist ../api/backend-dist/dist && cp -r node_modules ../api/backend-dist/node_modules",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "memory": 1024
    }
  }
}
