{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd backend && npx --yes pnpm@10.18.0 install --frozen-lockfile && cd .. && ln -sf backend/node_modules node_modules",
  "buildCommand": "cd backend && pnpm run build && cp -r dist ../api/backend-dist && ln -sf ../../backend/node_modules ../api/backend-dist/node_modules",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "backend/node_modules/**"
    }
  }
}
